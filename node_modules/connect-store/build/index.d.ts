import * as React from 'react';
export interface IObject {
    [key: string]: any;
}
export declare type TFieldWatcher = (field: string, value: any) => void;
export interface IStore extends IObject {
    __$__enhanced: boolean;
    __$__watchers: TFieldWatcher[];
    __$__refCount: number;
    __$__state: object;
    __idle?(): void;
}
export interface IDataSource {
    [key: string]: any;
}
export interface IDelegate {
    [key: string]: any;
}
export declare type TView<P> = React.ElementType<P>;
declare type Omit<T, K> = Pick<T, Exclude<keyof T, K>>;
export default class Connector<DS extends IDataSource, DG extends IDelegate, P extends {
    dataSource?: DS;
    delegate?: DG;
}> {
    private _View;
    private _dataSources;
    private _delegates;
    constructor(View: TView<P>);
    connect(store: DS & DG): this;
    dataSource(store: DS): this;
    delegate(delegate: DG): this;
    render(staticProps?: Omit<P, 'dataSource' | 'delegate'>, flushKeys?: string[]): JSX.Element;
}
export {};
